import { schema as attachBooks } from './attach-books-schema'
import { schema as attachExams } from './attach-exams-schema'
import { schema as createBookCourse } from './create-book-course-schema'
import { schema as updateBookCourse } from './update-book-course-schema'

export default {
  attachBooks,
  attachExams,
  createBookCourse,
  updateBookCourse,
}
