import { schema as createNotificationSchema } from './create-notification-schema'
import { schema as updateNotificationSchema } from './update-notification-schema'

export default {
  createNotificationSchema,
  updateNotificationSchema,
}
