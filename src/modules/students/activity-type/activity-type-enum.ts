export enum ActivityTypeEnum {
  books = 'books',
  vidoes = 'videos',
  test = 'tests',
  review = 'review',
  notes = 'notes',
  flashcards = 'flashcards',
  games = 'games',
}

export const ActivityTypes = Object.values(ActivityTypeEnum)
