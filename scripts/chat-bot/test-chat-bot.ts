// import { sendMessage } from '../../services/chat-bot/chat-bot'

// eslint-disable-next-line @typescript-eslint/no-floating-promises
(
  async () => {
    console.log('Testing AI chat bot')

    // await sendMessage({}, 'Hello, AI!', {})

    console.log('Done')

    process.exit(0)
  }
)()
